package com.example.galleryios18.feature.makestory

import android.graphics.Bitmap

class MediaEncoder(outputPath: String, width: Int, height: Int) {
    fun encode(bitmaps: List<Bitmap>) {
        // Sử dụng MediaCodec và MediaMuxer để mã hóa danh sách bitmap thành video MP4
    }
}
